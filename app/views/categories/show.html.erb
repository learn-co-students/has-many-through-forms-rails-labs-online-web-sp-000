<h2><%= @category.name %></h2> is associated with the following posts:
    <% @category.posts.each do |post| %>
       <ul> 
        <li><%= link_to post.title, post_path(post) %></li>
       </ul> 
        <% end %>