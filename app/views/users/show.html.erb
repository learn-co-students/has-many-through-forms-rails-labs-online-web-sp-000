<h2><%= @user.username %></h2>

<h3>Posts <%= @user.username %> has commented on:</h3>

<% @user.posts.uniq.each do |post| %>
    <ol>
        <li><%= link_to post.title, post_path(post) %></li>
    </ol>
<% end %>
