<%= form_for @post do |f| %>
    <%= f.text_field :title %>
    <%= f.text_area :content %>
    <%= f.collection_check_boxes :category_ids, Category.all, :id, :name, option: :include_blank %>
    <%= f.submit %>
<% end %>